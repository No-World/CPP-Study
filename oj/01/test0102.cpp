#include <iostream>
#include <math.h>
#define PI acos(-1)
using namespace std;
int main()
{
    cout << sin(PI / 6.0) * cos(PI / 12.0 * 5.0) + log(7.4) + pow(5.6, 4.0) << endl;
    cout << exp(6.0) - log10(65) + 3 * tan(PI * 11 / 60) << endl;
    return 0;
}