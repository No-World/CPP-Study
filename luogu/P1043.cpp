#include <cstdio>
#include <algorithm>
using namespace std;

int m, n;

int mod(int N, int M)
{
    if (N >= 0)
        return (N % M + M) % M;
}

int main()
{
    // printf("%d", mod(-1, 10));
    for (int i = 1; i <= n; i++)
    {
        scanf("%d", &);
    }

    return 0;
}